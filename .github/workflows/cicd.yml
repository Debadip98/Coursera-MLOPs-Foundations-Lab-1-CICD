name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15  # prevent runaway jobs

    steps:
<<<<<<< HEAD
      # --- Checkout the repository ---
      - uses: actions/checkout@v4

      # --- Set up Python ---
      - name: Set up Python 3.8
        uses: actions/setup-python@v5
        with:
          python-version: "3.8"
          cache: "pip"  # enables built-in pip caching!

      # --- Upgrade base build tools ---
      - name: Upgrade pip/setuptools/wheel
        run: |
          python -m pip install --upgrade pip setuptools wheel

      # --- Install dependencies efficiently ---
      - name: Install dependencies
        run: |
          # CI speedup: reuse cached wheels, prefer binaries
          pip install --prefer-binary -r requirements.txt
        env:
          PIP_NO_INPUT: 1
          PIP_PROGRESS_BAR: off

      # --- Lint, Test, Format, Deploy ---
      - name: Lint
        run: make lint

      - name: Run tests with coverage
        run: make test

      - name: Auto-format code
        run: make format

      - name: Deploy
        if: github.ref == 'refs/heads/main'
=======
      - uses: actions/checkout@v3

      - name: Set up Python 3.8
        uses: actions/setup-python@v4
        with:
          python-version: '3.8'

      - name: install packages
        run: make install

      - name: lint
        run: make lint

      - name: test
        run: make test

      - name: format
        run: make format

      - name: deploy
>>>>>>> 8a70ac90935d582313f415ebfd656e381a7ae232
        run: make deploy
